import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bolemedhanialem.mezmurtinat',
  appName: 'yetimket mezmurat',
  webDir: 'dist'
};

export default config;
